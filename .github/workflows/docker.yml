name: Build and Test App  # workflow name

on:
  push:
    branches: ["main"]   # triggers on push to main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4    # get the repo code

      - name: Build Docker Image
        run: docker build -t expense-app .
